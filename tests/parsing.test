# misc. testfile parsing tests
# 1. can we match / ?
./shelltest /dev/null >/dev/null | echo /
>>> /\//
# 2. can we handle a blank regexp ? with whitespace after ?
./shelltest /dev/null >/dev/null | echo
>>> //    
# 3. how about a comment after ?
./shelltest /dev/null >/dev/null | echo
>>> // # comment
# 4. no trailing newline after regexp ?
./shelltest /dev/null >/dev/null | printf ""
>>> //