# 1a. cat copies its input to stdout
<<<
foo
$$$ cat
>>>
foo

# 1b. echo prints a newline instead. We add an explicit >>>2 (or >>>=) to
# delimit this whitespace output.
$$$ echo
>>>

>>>2

# 2. cat prints an error containing "unrecognized option" and exits with
# non-zero status if given a bad flag
$$$ cat --no-such-flag
>>>2 /(unrecognized|illegal) option/
>>>= !0
